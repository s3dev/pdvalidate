language: python

python:
    - '3.4'
    - '3.5'

# whitelist
branches:
    only:
        - master

before_install:
    - pip install pytest pytest-cov

script:
    py.test --cov=./

after_success:
  - codecov